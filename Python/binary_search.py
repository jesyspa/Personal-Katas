def binary_search(li, item):
    """If item is in li, return the index.  Otherwise return -1.

    Parameters:
    - col is a sorted li.
    - item is a sought-for item.

    Each item in the list must be comparable with the sought-for item using
    operator<.
    
    """
    pass
